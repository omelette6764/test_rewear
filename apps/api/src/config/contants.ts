export const API_PREFIX = "v1";

export const DEFAULT_PAGE_LIMIT = 20;
export const MAX_PAGE_LIMIT = 50;

// Reservation policy
export const RESERVATION_MINUTES = 15;

// CORS defaults (tighten later)
export const DEFAULT_CORS = {
  origin: true,
  credentials: true,
};
